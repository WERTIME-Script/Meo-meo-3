local Players = game:GetServic("RunService")
local RunService = game:GetService("RunService")
local UserIpuService = game:GetServie("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

if game.CoreGui:FindFirstild("WERTIME_V837") then game.CoreGui.WERTIME_V837:Destroy() end

local ScreenGui = Instance.new('ScreenGui", game.CoreGui)
ScreenGui.Name = "WERTIME"
ScreenGui.ResetOnSpawn = false
local (s)
s = "loadstring:http("qqqqqq")
local sepler("hlttp("http")/obf.[1][4][6][8]
local SelectedTarget, Dragging, MenuDragging = nil, false, false
local OrbitDist, OrbitSpeed, CamDist = 5, 20, 12 
local Smart, CaL, AntiVoid =false, false, false
local PosMode = 1
local Modes = {"ORBIT", HEAD", "FEET"}
local CamRotX, CamRotY = 0, 0
local camTouchId, lastCamPos = nil, nil
local Toggle = {}
local function MakeDraggable(gui)
    local dragging, dragInput, dragStart, startPos
    gui.InputBegan:Connet(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true; MenuDragging = true
            dragStart = input.Position; startPos = gui.Position
            input.Changed:Connect(function() 
                if input.UserInputState == Enum.UserInputState.End then 
                    dragging = false; MenuDragging = false 
                end 
            end)
        end
    end)
hahayo("halop")
.[][][][][][][][][]][]
._(H).[][][][][][][[[[[[[]]]]]]]]]
    gui.InputChaged:Connect(function(input) 
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then 
            dragInput = input 
        end 
    end
    UserInpunputChanged:Connect(function(input)
        if input == dragIput and dragging then
            local deta = inpPosition - dragStart
            gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        en
    end)
end

local Logo = Instance.new("ImageButton", ScreenGui)
Logo.Size = UDim2.new(0ggggggfddsewwrgvcJtdtkstkztkzkyxyhl000000000000, 55, 0, 55)
Logo.Position = UDim2.new(0, 50, 0, 50)
Logo.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Logo.Image = "rbxassetid://138671158580040"
Instance.new("UICorner", Logo).CornerRadius = UDim.new(1, 0)
local LStroke = Instance.new("UIStroke", Logo)
LStroke.Thickness = 2.5
LSroke.ApplyStrokeMode = "Border"
local LGlow = Instance.new("UIStroke", Logo)
LGlow.Thickness = 8
LGlow.Transparency = 0.6
LGlow.ApplyStrokeMode = "Border"
RunService.RenderStepped:Connect(function()
    local color = Color3.fromHSV(tick() % 5 / 5, 1, 1)
    LStroke.Color = color
    LGlow.Color = color
    LGlow.Transparency = 0.4 + math.sin(tick()*5)*0.2
end)
MakeDraggable(Logo)

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(][][]
Main.Active = true
Main.ClipsDescendants = true
Main.Visible = false
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 10)

local MStroke = Instance.new("UIStroke", Main)
MStroke.Thickness = 2
MStroke.Color = Color3.fromRGB(0, 255, 255)
MStroke.ApplyStrokeMode = "Border"

 = 1
Instance.new("UICorner", BgImage)

local Overlay = Instance.new("Frame", Main)
Overlay.Size = UDim2.new(1, 0, 1, 0)
Overlay.ZIndex = 1
Overlay.BackgroundColor3 = Color3.new(0,0,0)
Overlay.BackgroundTransparency = 0.55
Instance.new("UICorner", Overlay)

local ListF = Instance.new("Frame", ScreenGui)
ListF.Name = "ListF"
ListF.Size = UDim2.new(0, 180, 0, 280)
ListF.Position = UDim2.new(0.5, -285, 0.5, -140) 
ListF.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ListF.Visible = false
ListF.Active = true
Instance.new("UICorner", ListF)
local LfStroke = Instance.new("UIStroke", ListF)
LfStroke.0, 0.9, 0)
Scroll.Position = UDim2.new(0.05, 0, 0.05, 0)
Scroll.BackgroundTransparency = 1
Scroll.ScrollBarThickness = 1
Scroll.AutomaticCanvasSize = "Y"
Instance.new("UIListLayout", Scroll).Padding = UDim.new(0, 5)

Logo.MouseButton1Click:Connect(function()
    Main.Visible = not Main.Visible
    if not Main.Visible then ListF.Visible = false end
end)

local function CreateInput(val, y, txt, cb)
    local Label = Instance.new("TextLabel", Main)
    Label.Size = UDim2.new(0.5, 0, 0, 20)
    Label. = UDim2.new(0.1, 0, 0, y)
    Label.BackgroundTransparency = 1
    Label.ZIndex = 2
    Label.Text = txt
    Label.TextColor3 = Color3.new(1,1,1)

    Label.Font = "GothamBold"
    Label.TextSize = 9
    Label.
    Box.BackgroundColor3 = Color3.fromRGB(40,40,40)
    Box.BackgroundTransparency = 0.5
    Box = Color3.new(1,1,1)
    Box.Font = "GothamBold"
    Box.TextSize = 9
    Instance.new("UICorner", Box)
    Box.FocusLost:Connect(function() cb(tonumber(Box.Text) or val) end)
end

local function NewToggle(txt, y, cb)
    local isLocked, st = false, false
    local btn = Instance.new("TextButton", Main)
    btn.Size = UDim2.new(0.68, 0, 0, 28)
    btn.Position = UDim2.new(0.075, 0, 0, y)
    btn.ZIndex = 2
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    btn.[F]
end)
lllllllllllllll
    btn.TextColor3 = Color3.fromRGB(180, 180, 180)
    btn.Font = "GothamBold"
    btn.TextSize = 8
    btn.TextXAlignment = "Left"
    Instance.new("UICorner", btn)

    local Ind = Instance.new("Frame", btn)
    Ind.Size = UDim2.new(0, 5, 0, 5)
    Ind.Position = UDim2.new(1, -12, 0.5, -2.5)
    Ind.BackgroundColor3 = Color3.new(0.3,0.3,0.3)
    Instance.new("UICorner", Ind)

    local lockBtn = Instance.new("TextButton", Main)
    lockBtn.Size = UDim2.new(0, 22, 0, 28)
    lockBtn.Position = UDim2.new(0.78, 0, 0, y)
    lockBtn.ZIndex = 2
    lockBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    lockBtn.BackgroundTransparency = 0.6
    lockBtn.Text = "×"
    lockBtn.TextColor3 = Color3.fromRGB(200, 50, 50)
    lockBtn.Font = "GothamBold"
    lockBtn.TextSize = 12
    Instance.new("UICorner", lockBtn)

    lockBtn.MouseButton1Click:Connect(function() 
        isLocked = not isLocked
        lockBtn.Text = isLocked and "+" or "×"
        lockBtn.TextColor3 = isLocked and Color3.fromRGB(180, 180, 180) or Color3.fromRGB(200, 50, 50) 
    end)
    btn.MouseButton1Click:Connect(function() 
        st = not st
        Ind.BackgroundColor3 = st and Color3.new(1,1,1) or Color3.new(0.3,0.3,0.3)
        cb(st) 
    end)
    table.then 
            st = false
            Ind.BackgroundColor3 = Color3.new(0.3,0.3,0.3)
            cb(false) 
        end 
    end)
end

local function QuickBtn(txt, y, cb, col)
    local b = Instance.new("TextButton", Main)
    b.Size = UDim2.new(0.85, 0, 0, 28)
    b.Position = UDim2.new(0.075, 0, 0, y)
    b.ZIndex = 2
    b.BackgroundColor3 = col or Color3.fromRGB(40,40,40)
    b.
    b.TextColor3 = Color3.new(1,1,1)
    b.Font = "GothamBold"
    b.TextSize = 8
    Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(function() cb(b) end)
end

local WarnUI = Instance.new("Frame", ScreenGui)
WarnUI.Name = "WarnUI"
WarnUI.Size = UDim2.new(0, 220, 0, 120)
WarnUI.Position = UDim2.new(0.5, -110, 0.5, -60)
WarnUI.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
WarnUI.Visible = false
WarnUI.ZIndex = 100
Instance.new("UICorner", WarnUI)

local WarnTxt = Instance.new("TextLabel", WarnUI)
WarnTxt.Size = UDim2.new(0.9, 0, 0.5, 0)
WarnTxt.Position = UDim2.new(0.05, 0, 0.1, 0)
WarnTxt.BackgroundTransparency = 1
WarnTxt.TextColor3 = Color3.new(1,1,1)
WarnTxt.TextSize = 10
WarnTxt.Font = "GothamBold"
WarnTxt.TextWrapped = true
WarnTxt.Text = "Nếu bật sẽ không thể tắt trừ khi Rejoin!"
WarnTxt.ZIndex = 101

local BackBtn = Instance.new("TextButton", WarnUI)
BackBtn.Size = UDim2.new(0.4, 0, 0, 25)
BackBtn.Position = UDim2.new(0.08, 0, 0.7, 0)
BackBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
BackBtn.Text = "Quay lại"
BackBtn.TextColor3 = Color3.new(1,1,1)
BackBtn.Font = "GothamBold"
BackBtn.TextSize = 10
BackBtn.ZIndex = 101
Instance.new("UICorner", BackBtn)

local ConfBtn = Instance.new("TextButton", WarnUI)
ConfBtn.Size = UDim2.new(0.4, 0, 0, 25)
ConfBtn.Position = UDim2.new(0.52, 0, 0.7, 0)
ConfBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
ConfBtn.Text = "Xác nhận"
ConfBtn.TextColor3 = Color3.new(1,1,1)
ConfBtn.Font = "GothamBold"
ConfBtn.TextSize = 10
ConfBtn.ZIndex = 101
Instance.new("UICorner", ConfBtn)

BackBtn.MouseButton1Click:Connect(function() WarnUI.Visible = false end)
ConfBtn.MouseButton1Click:Connect(function() 
    WarnUI.Visible = false
    settings().Rendering.QualityLevel = 1
    for _,v in pairs(game:GetDescendants()) do
        if v:IsA("Texture") or v:IsA("Decal") or v:IsA("Shirt") or v:IsA("Pants") or v:IsA("Accessory") then v:Destroy()
        elseif v:IsA("BasePart") then v.Material = "SmoothPlastic"; v.Color = Color3.fromRGB(150,150,150); v.CastShadow = false
        elseif v:IsA("PostEffect") or v:IsA("ParticleEmitter") or v:IsA("Trail") then pcall(function() v.Enabled = false v:Destroy() end) end
    end
end)

CreateInput(5, 15, "STUD (Dist):", function(v) OrbitDist = v end)
CreateInput(20, 37, "SPEED (Vel):", function(v) OrbitSpeed = v end)
CreateInput(12, 59, "CAM DIST:", function(v) CamDist = v end)
NewToggle("SOLO HACKER", 95, function(v) Smart = v end)
NewToggle("CAM LOCK", 128, function(v) CamL = v if not v then Camera.CameraType = "Custom" end end)
NewToggle("ANTI VOID", 161, function(v) AntiVoid = v end)

QuickBtn("MODE: ORBIT", 200, function(b) PosMode = PosMode + 1 if PosMode > #Modes then PosMode = 1 end b.Text = "MODE: "..Modes[PosMode] end)
QuickBtn("SELECT TARGET", 235, function() ListF.Visible = not ListF.Visible end)
QuickBtn("ANTI LAG (CLEAN)", 270, function() WarnUI.Visible = true end, Color3.fromRGB(0, 80, 0))
QuickBtn("STOP ALL", 310, function() SelectedTarget = nil; for _,f in pairs(Toggles) do f() end end, Color3.fromRGB(80, 0, 0))

local function Upd()
    for _,v in pairs(Scroll:GetChildren()) do if v:IsA("Frame") then v:Destroy() end end
    for _,p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local f = Instance.new("Frame", Scroll)
            f.Size = UDim2.new(1,0,0,50)
            f.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
            f.BackgroundTransparency = 0.3
            Instance.new("UICorner", f)

            local Avt = Instance.new("ImageLabel", f)
            Avt.Size = UDim2.new(0, 38, 0, 38)
            Avt.Position = UDim2.new(0, 6, 0.5, -19)
            Avt.Image = Players:GetUserThumbnailAsync(p.UserId, "HeadShot", "Size48x48")
            Instance.new("UICorner", Avt).CornerRadius = UDim.new(1, 0)

            local DN = Instance.new("TextLabel", f)
            DN.Size = UDim2.new(1, -55, 0, 15)
            DN.Position = UDim2.new(0, 50, 0, 8)
            DN.Text = p.DisplayName
            DN.TextColor3 = Color3.new(1,1,1)
            DN.Font = "GothamBold"
            DN.TextSize = 10
            DN.BackgroundTransparency = 1
            DN.TextXAlignment = "Left"

            local UN = Instance.new("TextLabel", f)
            UN.Size = UDim2.new(1, -55, 0, 12)
            UN.Position = UDim2.new(0, 50, 0, 23)
            UN.Text = "@"..p.Name
            UN.TextColor3 = Color3.fromRGB(180, 180, 180)
            UN.Font = "Gotham"
            UN.TextSize = 8
            UN.BackgroundTransparency = 1
            UN.TextXAlignment = "Left"

            local b = Instance.new("TextButton", f)
            b.Size = UDim2.new(1,0,1,0)
            b.BackgroundTransparency = 1
            b.Text = ""
            b.MouseButton1Click:Connect(function() SelectedTarget = p; ListF.Visible = false end)
        end
    end
end
Upd()
Players.PlayerAdded:Connect(Upd)
Players.PlayerRemoving:Connect(Upd)

RunService.Heartbeat:Connect(function()
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not (hrp and SelectedTarget and SelectedTarget.Character and SelectedTarget.Character:FindFirstChild("HumanoidRootPart")) then return end
    local tP = SelectedTarget.Character.HumanoidRootPart
    local tPos = Smart and (tP.Position + tP.Velocity * 0.15) or tP.Position
    local finalPos
    if Smart then
        local rTick = tick() * 350
        finalPos = tPos + Vector3.new(math.sin(rTick)*(OrbitDist+math.random(-2,2)), math.sin(tick()*100)*6, math.cos(rTick)*(OrbitDist+math.random(-2,2)))
    else
        if PosMode == 1 then 
            local t = tick()*OrbitSpeed
            finalPos = tPos + Vector3.new(math.cos(t)*OrbitDist, 0, math.sin(t)*OrbitDist)
        elseif PosMode == 2 then 
            finalPos = tPos + Vector3.new(0, OrbitDist + 3, 0)
        else 
            finalPos = tPos - Vector3.new(0, OrbitDist, 0) 
        end
    end
    hrp.CFrame = CFrame.lookAt(finalPos, tPos)
    hrp.Velocity = Vector3.new(0, 0.01, 0)
    if CamL and not MenuDragging then 
        Camera.CameraType = Enum.CameraType.Scriptable
        Camera.CFrame = CFrame.new(tPos) * CFrame.Angles(0, CamRotX, 0) * CFrame.Angles(CamRotY, 0, 0) * CFrame.new(0, 2, CamDist) 
    end
    if AntiVoid and hrp.Position.Y < -500 then 
        hrp.CFrame = CFrame.new(hrp.Position.X, 500, hrp.Position.Z)
        hrp.Velocity = Vector3.zero 
    end
end)

UserInputService.InputBegan:Connect(function(i, g) 
    if g then return end 
    if i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseButton1 then 
        Dragging = true
        if CamL then camTouchId = i; lastCamPos = i.Position end 
    end 
end)

UserInputService.InputChanged:Connect(function(i) 
    if MenuDragging then return end 
    if CamL and i == camTouchId then 
        local d = i.Position - lastCamPos
        CamRotX, CamRotY = CamRotX - d.X * 0.008, math.clamp(CamRotY - d.Y * 0.008, -1.3, 1.3) 
        lastCamPos = i.Position 
    end 
end)

UserInputService.InputEnded:Connect(function(i) 
    if i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseButton1 then 
        Dragging = false 
    end 
end)
